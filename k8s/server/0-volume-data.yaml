apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-pv-pi-hole-data
  labels:
    volume: pi-hole-data
spec:
  capacity:
    storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  hostPath:
    path: "/mnt/kube-data/pi-hole/app-data"