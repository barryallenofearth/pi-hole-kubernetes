apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-pi-hole-data-pvc
  namespace: pi-hole
spec:
  selector:
    matchLabels:
      volume: pi-hole-data
  accessModes: [ "ReadWriteOnce" ]
  storageClassName: ""
  resources:
    requests:
      storage: 10Gi