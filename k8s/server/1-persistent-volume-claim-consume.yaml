apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-pi-hole-data-pvc
  namespace: pi-hole
spec:
  selector:
    matchLabels:
      volume: pi-hole-data
  accessModes: [ "ReadWriteMany" ]
  storageClassName: ""
  resources:
    requests:
      storage: 10.Gi